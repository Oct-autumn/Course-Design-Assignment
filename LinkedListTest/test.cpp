#include <stdio.h>
#define PATH_LONG 1024

typedef struct linkNode {
	char filepath[PATH_LONG];
	struct linkNode *next;
};

int main() {

	return 0;
}

linkPoint *NewNode(char filepath_save, linkNode *BeforeNode, linkNode *NextNode) {

	linkNode *p = NULL;
	return p;
}